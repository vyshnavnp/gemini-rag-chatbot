__pycache__
*.pyc
.env
.venv
venv
.git
.DS_Store
*.log
# Exclude the entire chroma_db directory from the image.
# A fresh DB is always built at container startup by the background indexer
# reading from /app/knowledge_base (which IS baked into the image).
# The chroma_db volume in docker-compose.yml persists it across restarts.
chroma_db/
# Training artefacts — not needed at runtime
notebooks/
datasets/
# Misc dev files
_count_lines.py
index_metadata.json
*.md
!README.md